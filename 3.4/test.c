# include<stdio.h>
int main()
{
    char str1 = 'H';
    // int sz = sizeof(str1) / sizeof(str1[0]);
    // printf("%d\n", sz);

    switch (str1)
    {
        case 'H':
            printf("%d",1);
            break;
    }

    return 0;
}